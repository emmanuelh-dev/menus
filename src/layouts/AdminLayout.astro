---
// import BaseHead from '../components/BaseHead.astro';
// import { supabase } from '../lib/supabase';
// import { getUser } from '../lib/auth';

// const user = await getUser();
// if (!user) {
//   return Astro.redirect('/login');
// }
---
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body class="min-h-screen bg-gray-50">
    <div class="flex h-screen">
      <!-- Sidebar -->
      <aside class="w-64 bg-white shadow-md">
        <div class="p-4 border-b">
          <h1 class="text-xl font-serif font-bold text-[#3A2E27]">Administración</h1>
        </div>
        <nav class="p-4 space-y-2">
          <a
            href="/admin/restaurants"
            class="block px-4 py-2 text-gray-700 hover:bg-[#A67C52] hover:text-white rounded-md transition-colors"
          >
            Restaurantes
          </a>
        </nav>
      </aside>

      <!-- Main Content -->
      <main class="flex-1 overflow-auto">
        <!-- Header -->
        <header class="bg-white shadow-sm">
          <div class="flex justify-between items-center px-8 py-4">
            <h2 class="text-lg font-medium text-gray-700">
              <!-- Bienvenido, {user.email} -->
            </h2>
            <form action="/api/auth/signout" method="POST">
              <button
                type="submit"
                class="px-4 py-2 text-sm text-white bg-[#A67C52] hover:bg-[#8C5E3B] rounded-md transition-colors"
              >
                Cerrar Sesión
              </button>
            </form>
          </div>
        </header>

        <!-- Page Content -->
        <div class="p-8">
			<slot />
        </div>
      </main>
    </div>
  </body>
</html>