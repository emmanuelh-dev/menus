// Define the interface for contact form data
export interface Contact {
  email: string;
  message: string;
}

// Define the interface for the contact form status
export interface ContactFormStatus {
  success: boolean;
  message: string;
}