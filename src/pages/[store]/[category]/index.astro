---
export const prerender = false;

const { store, category } = Astro.params;

if (!store || !category) {
  throw new Error('Ruta inválida');
}

return Astro.redirect(`/tienda/${encodeURIComponent(store)}/${encodeURIComponent(category)}`);
---
