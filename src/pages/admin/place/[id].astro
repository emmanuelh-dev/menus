---
import AdminLayout from "../../../layouts/AdminLayout.astro";
import PlaceEditContainer from "../../../components/admin/PlaceEditContainer";

const { id } = Astro.params;

// Esta página es una concha (shell) estática.
// La verificación de autenticación y carga de datos se realiza
// en el cliente para una respuesta instantánea.
---

<AdminLayout title={`Editar | BysMax`}>
	<PlaceEditContainer client:only="react" placeId={id!} />
</AdminLayout>
